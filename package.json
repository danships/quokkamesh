{
  "name": "quokkamesh",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "QuokkaMesh monorepo — P2P agent mesh",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r test:watch",
    "lint": "pnpm -r run lint",
    "lint:eslint": "pnpm -r run lint:eslint",
    "lint:prettier": "pnpm -r run lint:prettier",
    "format": "pnpm -r run format",
    "prepare": "husky",
    "lint-staged": "lint-staged"
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint --max-warnings=0",
      "prettier --check"
    ]
  },
  "devDependencies": {
    "potrace": "^2.1.8",
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^10.0.0",
    "eslint-plugin-unicorn": "^58.0.0",
    "globals": "^16.0.0",
    "husky": "^9.0.0",
    "lint-staged": "^16.2.7",
    "prettier": "^3.0.0",
    "typescript-eslint": "^8.0.0"
  },
  "license": "MIT"
}
